<h1>投稿一覧</h1>

<% @posts.each do |post| %>
    <%= post.product_image %>
    <%= post.product %>
    <%= post.price %>
    <%= post.product_comment %>
    <%= link_to '詳しく見る', post_path(post) %>
    <%= link_to "削除", post_path(post.id), method: :delete %>
<% end %>